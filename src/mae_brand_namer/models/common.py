"""Common model classes and utilities."""

# Import and re-export the Dependencies class from the config module
# This is for backward compatibility with code that expects it to be in models.common
from mae_brand_namer.config.dependencies import Dependencies, create_dependencies

# Add any other common model classes or utilities here as needed 